<%= form_with(url: sessions_path) do |form| %> <!-- Con la url llamamos a la opcion create de sessions -->
  <div>
    <%= form.label :login %>
    <%= form.text_field :login, required: true %>
  </div>

  <div>
    <%= form.label :password %>
    <%= form.password_field :password, required: true %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
